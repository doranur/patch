<div id="post_show">
  <h1>
    <%= @post.title %>
  </h1>
  <p class="username">
    Shared by
    <%= @post.user.name %>
    about
    <%= time_ago_in_words(@post.created_at) %>
  </p>
  <div class="clearfix">
    <div class="post_image_description">
        <%= image_tag(@post.link, class: 'postImage')  %>
      <div class="description">
        <%= simple_format(@post.description) %>
      </div>
    </div>
    <div class="post_data">
      <%= link_to "Visit Link", @post.link, target: "_blank", class: "button" %>
      <%= link_to like_post_path(@post), method: :get, class: "data" do %>
        <i class="fa fa-thumbs-o-up"></i>
        <%= pluralize(@post.get_upvotes.size, "Like") %>
      <% end %>
      <p class="data">
        <i class="fa fa-comments-o"></i>
        <%= pluralize(@post.comments.count, "Comment") %>
      </p>
      <% if @post.user == current_user %>
        <%= link_to "Edit", edit_post_path(@post), class: "data" %>
        <%= link_to "Delete", post_path(@post), method: :delete, data: { confirm: "Are you sure?" }, class: "data" %>
      <% end %>
    </div>
  </div>
</div>

<!-- <div id="random_post">
  <h3>Random Inspiration</h3>
  <div class="post">
    <div class="post_image">
      <%= link_to image_tag(@random_post.link, class: 'random'), post_path(@random_post) %>
    </div>
    <div class="post_content">
      <div class="title">
        <h2>
          <%= link_to @random_post.title, post_path(@random_post) %>
        </h2>
      </div>
      <div class="data clearfix">
        <p class="username">
          Shared by
          <%= @random_post.user.name %>
        </p>
        <p class="buttons">
          <span>
            <i class="fa fa-comments-o"></i>
            <%= @random_post.comments.count %>
          </span>
          <span>
            <i class="fa fa-thumbs-o-up"></i>
            <%= @random_post.get_likes.size %>
          </span>
        </p>
      </div>
    </div>
  </div>
  <div class="post">
    <div class="post_image">
      <%= link_to image_tag(@random_post2.link, class: 'random'), post_path(@random_post2) %>
    </div>
    <div class="post_content">
      <div class="title">
        <h2>
          <%= link_to @random_post2.title, post_path(@random_post2) %>
        </h2>
      </div>
      <div class="data clearfix">
        <p class="username">
          Shared by
          <%= @random_post2.user.name %>
        </p>
        <p class="buttons">
          <span>
            <i class="fa fa-comments-o"></i>
            <%= @random_post2.comments.count %>
          </span>
          <span>
            <i class="fa fa-thumbs-o-up"></i>
            <%= @random_post2.get_likes.size %>
          </span>
        </p>
      </div>
    </div>
  </div>
  <div class="post">
    <div class="post_image">
      <%= link_to image_tag(@random_post3.link, class: 'random'), post_path(@random_post3) %>
    </div>
    <div class="post_content">
      <div class="title">
        <h2>
          <%= link_to @random_post3.title, post_path(@random_post3) %>
        </h2>
      </div>
      <div class="data clearfix">
        <p class="username">
          Shared by
          <%= @random_post3.user.name %>
        </p>
        <p class="buttons">
          <span>
            <i class="fa fa-comments-o"></i>
            <%= @random_post3.comments.count %>
          </span>
          <span>
            <i class="fa fa-thumbs-o-up"></i>
            <%= @random_post3.get_likes.size %>
          </span>
        </p>
      </div>
    </div>
  </div>
</div> -->

<div class="horizontal"></div>
<h2>
    <%= comment_count = pluralize(@post.comments.count, "Comment") %>:
</h2>
<% @comments.each do |comment| %>
<p><%= username = comment.user.name %>:
<%= content = comment.content %>
<% if user_signed_in? && current_user.id == comment.user.id %>
<%= link_to '(Delete)', [comment.post, comment],
              method: :delete,
              data: { confirm: 'Are you sure you want to delete this comment?' } %></p>
<% else %>
<% end %>
<% end %>
</p>

<p></p>
<%= render 'comments/form' %>
