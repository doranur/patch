<h1>Post Index</h1>

  <% @posts.each do |post| %>
      <p><%= link_to post.title, post_path(post) %></p>
      <p><%= link_to image_tag(post.link, class: 'thumb'), post_path(post) %></p>
      <p><%= post.get_likes.size %>Likes</p>
  <% end %>

<%= link_to 'New Post', new_post_path %>
